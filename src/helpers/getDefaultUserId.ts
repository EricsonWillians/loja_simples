// Using a default one since I'm not sure I'll have time to implement
// the whole user sign up / login system
const defaultUserId = "1";

const getDefaultUserId = () => {
  return defaultUserId;
};

export default getDefaultUserId;
